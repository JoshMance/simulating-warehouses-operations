#include "model.hpp"

int main() {

    int shelf_size_row = 1;
    int shelf_size_col = 1;
    int gap_size_row = 2;
    int gap_size_col = 2;
    int num_shelves_row = 1;
    int num_shelves_col = 3;
    int num_agents = 5;

    //Model model(shelf_size_row, shelf_size_col, gap_size_row, gap_size_col,
                //num_shelves_row, num_shelves_col, num_agents);

    

    return 0;
}
